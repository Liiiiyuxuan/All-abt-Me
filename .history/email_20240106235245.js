function myFunction() {
     // Copy the text inside the text field
    navigator.clipboard.writeText("liiiiyuxuan@gmail.com");

    // Alert the copied text
    tooltip.innerHTML = "Email Copied";
}

function outFunc() {
    tooltip.innerHTML = "Copy to clipboard";
}